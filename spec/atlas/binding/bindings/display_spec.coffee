efs = tests.dom.elementFromString

describe 'Display binding', ->
  describe 'with display only eelments', ->
    # div, span, p, etc. - can modify at atlas.binding.displayOnlyElements
    it 'correctly sets values', ->
    it 'correctly gets values', ->

  describe 'with '